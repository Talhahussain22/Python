#__str__ method is one of magic or dunder method it is used to show some string result whenever we print instance
#or object of class generally when we print class which does not have this __str__ method it show location of object in memory
from emp_class_for_import import Employee
c=Employee("Talha")
print(c)
print(len(c))
